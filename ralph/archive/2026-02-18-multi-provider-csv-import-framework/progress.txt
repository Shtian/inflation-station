## Codebase Patterns
- Keep primary app navigation in `src/app/layout.tsx`, and keep `/` redirecting to `/overview`.
- Keep API handlers in `src/app/api/**` and use the shared server-only Prisma client in `src/lib/prisma.ts`.
- Use shadcn table primitives from `src/components/ui/table.tsx` for tabular UI instead of ad-hoc table markup.
- For paginated table routes, keep summary metadata (`page`, `totalPages`, `total`) outside the table and keep row rendering inside table primitives.
- Keep route page state client-side and fetch route-specific API data on filter/pagination changes.
- Keep transactions pagination/filter query logic in `src/lib/transactions/*`, and keep `/api/transactions` handlers focused on request validation and HTTP response shaping.
- Keep `/api/transactions/[transactionId]` handlers consistent across mutation methods by validating `transactionId` up front and mapping Prisma `P2025` to `TRANSACTION_NOT_FOUND`.
- Keep transactions route filter state client-side in `src/app/transactions/*`, and refetch `/api/transactions` whenever account, page, or pageSize changes.
- Keep `/api/transactions` list rows enriched with `categoryName` so `/transactions` can render category labels (or uncategorized states) without extra category lookups.
- Keep transaction edit UI state local in `src/app/transactions/*` and refresh list data via the same paginated query after successful saves to preserve active filter/page context.
- Keep the transactions edit workflow NOK-only by removing currency from edit payload parsing/UI and forcing persisted updates to `currency: "NOK"`.
- Keep transaction delete confirmation state local in `src/app/transactions/*` and refetch the paginated list after delete so the UI can recover from last-row-on-page removals.
- For Next.js dynamic API routes, accept `params` as `Promise<unknown>` in the handler signature and narrow locally before using route ids.
- For Next.js App Router handlers, return `new Response(null, { status: 204 })` for no-content responses; `NextResponse.json` is invalid for HTTP 204.
- Keep Playwright configured for base URL `http://127.0.0.1:3000` and use `PLAYWRIGHT_NO_WEBSERVER=1` when you need to force reuse of an already running local dev server.
- Keep `pnpm test` as the full verification entry point (unit + E2E) and use `pnpm test:unit` for fast Vitest-only loops.
- Store provider import mappings in Prisma using a parent mapping model plus child field-mapping rows, with JSON normalization/transform payloads and `mappingVersion` metadata for diagnostics.
- For provider autodetection, read configurable `requiredHeaders`/`anyHeaders`/`headerPatterns` from mapping JSON, fall back to mapped source fields, and return parse diagnostics with a `certain`/`uncertain`/`missing` confidence state.
- When `/api/imports/parse` detection is not `certain` and provider candidates exist, return `PROVIDER_SELECTION_REQUIRED` with diagnostics and require a follow-up parse call with explicit `providerId`.
- For provider-backed parsing, transform raw CSV through provider field mappings into canonical parser rows before review staging, and keep parser summary keys exactly `imported`, `duplicates`, `ignoredReserved`, `invalid`.
- Keep import token/payment-type normalization centralized in `src/lib/import/normalization.ts` and apply it before any fingerprint dedupe or duplicate-warning computation.
- Keep review-stage OpenAI message cleanup best-effort and expose unavailable diagnostics with stable reason codes (`disabled`, `key_missing`, `timeout`, `provider_error`) without blocking parse staging.
- Keep provider mapping admin CRUD in `/api/import-provider-mappings*` and reject create/update payloads that omit any required canonical field mapping.
- Keep provider mapping admin UI state local in `src/app/import-provider-mappings/*` and submit create/update payloads as full mapping definitions (provider metadata + field mappings + normalization rules JSON) so API validation feedback stays deterministic.
- For shadcn select-based row controls, set `aria-label` on `SelectTrigger` so per-row Playwright locators stay unambiguous when multiple comboboxes exist in the same table row.
- Keep `/api/imports/submit` payload rows carrying finalized `selectedMessage` text, and recompute `normalizedMerchant` from staged `name` + selected message before dedupe/persistence.

## 2026-02-17 13:53 CET - US-001
- What was implemented
  - Added Prisma models for provider mapping definitions: `ImportProviderMapping` (provider name, normalization rules JSON, mapping version metadata) and `ImportProviderFieldMapping` (source-to-canonical mappings plus optional transform rules JSON).
  - Added relational constraints/indexes for deterministic lookup and canonical-field uniqueness per provider mapping.
  - Generated and applied migration `20260217125021_add_import_provider_mappings`, then regenerated Prisma client.
  - Fixed pre-existing lint/typecheck/test drift required to pass repo quality gates (`src/components/ui/tabs.tsx`, `src/lib/deterministic-color.ts`, and dashboard E2E tab assertions).
- Files changed
  - `prisma/schema.prisma`
  - `prisma/migrations/20260217125021_add_import_provider_mappings/migration.sql`
  - `src/components/ui/tabs.tsx`
  - `src/lib/deterministic-color.ts`
  - `tests/e2e/us-012-category-review-ui.e2e.ts`
  - `tests/e2e/us-014-dashboard-ui.e2e.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm exec prisma generate` (pass)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep provider mapping schema split between provider-level metadata and per-field mapping rows so detection/parse logic can evolve without schema churn.
    - Preserve mapping diagnostics in schema with explicit version metadata (`mappingVersion`) and timestamps.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Overview dashboard assertions in Playwright must switch tabs before expecting category-tab content; those headings/charts are not visible in the default `Flow` tab.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Prisma migration `20260217125021_add_import_provider_mappings` is now the baseline for upcoming provider detection and mapping API stories.
---

## 2026-02-17 14:28 CET - US-002
- What was implemented
  - Added provider detection service in `src/lib/import/provider-detection.ts` that evaluates mappings with configurable header/pattern rules and score-based confidence.
  - Wired `/api/imports/parse` to return `detection` diagnostics with detected provider id/name, confidence state, score, matched headers, and ranked candidates.
  - Added focused unit coverage for certain/uncertain/missing detection and source-field fallback behavior.
- Files changed
  - `src/lib/import/provider-detection.ts`
  - `src/lib/import/provider-detection.test.ts`
  - `src/app/api/imports/parse/route.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep provider detection logic in `src/lib/import/*` and keep `/api/imports/parse` focused on payload validation plus response shaping.
    - Keep detection confidence deterministic by combining required-header coverage with optional/pattern signal and ranking candidates.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - CSV files without a recognizable header row should return `missing` detection diagnostics instead of forcing a low-confidence provider guess.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Detection currently informs diagnostics only; canonical row transformation is still handled by the existing parser and staging flow.
---

## 2026-02-17 14:54 CET - US-003
- What was implemented
  - Extended `/api/imports/parse` payload parsing to accept optional `providerId` from both multipart form-data and JSON.
  - Updated parse flow to run provider detection first, return `409 PROVIDER_SELECTION_REQUIRED` with diagnostics when detection is `uncertain`/`missing` and candidates exist, and only stage review rows once parse is allowed.
  - Added provider override handling in parse route: validates selected provider, uses it instead of auto-detection, and returns the selected provider in diagnostics.
  - Updated `/import` UI to include provider selection (auto-detect + detected candidates), show detection status, and re-parse with override when required.
  - Added Playwright E2E coverage for uncertain detection requiring manual provider selection before parsing can continue.
- Files changed
  - `src/app/api/imports/parse/route.ts`
  - `src/app/import/import-uploader.tsx`
  - `tests/e2e/us-016-import-route-upload.e2e.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep ambiguous-provider handling in the parse API contract (`PROVIDER_SELECTION_REQUIRED` + detection payload) so UI can gate progression deterministically.
    - Keep provider override state in `/import` client state and submit it via multipart `providerId` to avoid introducing separate provider preflight endpoints.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - `/import` parse responses now require a `detection` payload; mocked API responses in E2E tests must include it to avoid runtime failures.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Existing import review/submit flow remains unchanged once parse succeeds; this story only gates parse progression when provider detection is ambiguous.
---

## 2026-02-17 15:03 CET - US-004
- What was implemented
  - Added provider-mapped CSV parser (`src/lib/import/provider-csv-parser.ts`) that loads configured source-to-canonical field mappings and transforms provider rows into the existing canonical import row shape.
  - Added structured row-level diagnostics for mapped rows (invalid amount/currency and missing required mapped values) while preserving parser summary keys as `imported`, `duplicates`, `ignoredReserved`, `invalid`.
  - Updated review staging to parse with provider mappings when available and keep existing fallback parsing for non-provider flows.
  - Updated `/api/imports/parse` to load the selected/detected provider mapping with field mappings and pass it into staging.
  - Added unit coverage for provider parser behavior and review-stage provider transformation path.
- Files changed
  - `src/lib/import/provider-csv-parser.ts`
  - `src/lib/import/provider-csv-parser.test.ts`
  - `src/lib/import/review-stage.ts`
  - `src/lib/import/review-stage.test.ts`
  - `src/app/api/imports/parse/route.ts`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep provider CSV parsing isolated in `src/lib/import/*` and pass resolved provider mapping data from parse route into staging.
    - Keep provider transformation output aligned with the canonical parser row contract so downstream dedupe/review logic remains unchanged.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Biome enforces `indexOf` for direct header equality lookups and sorted import specifiers in import blocks.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Provider mapping parser currently supports both semicolon and comma delimiters with quoted-cell handling.
---

## 2026-02-17 15:13 CET - US-005
- What was implemented
  - Added shared import normalization helpers in `src/lib/import/normalization.ts` for deterministic token folding (including Nordic characters), payment-type normalization to Prisma `PaymentType`, and merchant normalization.
  - Updated fingerprint dedupe flow to normalize provider payment labels before fingerprint generation in `src/lib/import/transaction-dedupe.ts`.
  - Updated import persistence (`src/lib/import/import-transactions.ts`) and review staging (`src/lib/import/review-stage.ts`) to reuse shared normalization before dedupe and duplicate-warning checks.
  - Added focused unit coverage validating `Overføring` + Nordic merchant text normalization for both import dedupe and review duplicate-warning behavior.
- Files changed
  - `src/lib/import/normalization.ts`
  - `src/lib/import/transaction-dedupe.ts`
  - `src/lib/import/import-transactions.ts`
  - `src/lib/import/review-stage.ts`
  - `src/lib/import/transaction-dedupe.test.ts`
  - `src/lib/import/import-transactions.test.ts`
  - `src/lib/import/review-stage.test.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep import normalization logic centralized to avoid drift between parse/review/submit dedupe paths.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Running heavy checks in parallel (`pnpm test` with `pnpm build`) can cause transient Playwright timeouts; run `pnpm test` in isolation for stable E2E verification.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Duplicate warnings in review are computed from normalized fingerprint fields and intentionally mark all matching upload rows as potential duplicates.
---

## 2026-02-17 15:40 CET - US-006
- What was implemented
  - Added provider mapping admin CRUD APIs with validation: `GET`/`POST` at `/api/import-provider-mappings` and `PATCH`/`DELETE` at `/api/import-provider-mappings/[providerMappingId]`.
  - Enforced payload validation for create/update flows, including duplicate canonical-field detection and required canonical-field coverage checks before persistence.
  - Preserved provider normalization rules and `mappingVersion` metadata through create/update API paths.
  - Added focused API route unit tests for payload validation, required-field rejection, successful create/update, and not-found delete handling.
- Files changed
  - `src/app/api/import-provider-mappings/route.ts`
  - `src/app/api/import-provider-mappings/route.test.ts`
  - `src/app/api/import-provider-mappings/[providerMappingId]/route.ts`
  - `src/app/api/import-provider-mappings/[providerMappingId]/route.test.ts`
  - `src/lib/import/provider-mapping-contract.ts`
  - `src/lib/import/json-value.ts`
  - `src/lib/import/provider-csv-parser.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep required canonical provider-field definitions centralized so provider parsing and provider-admin API validation stay aligned.
    - Replace provider field mappings atomically on update (`deleteMany` + `create`) to keep mapping payloads deterministic.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Provider mapping JSON payloads should be validated as JSON-serializable input before Prisma writes to avoid `unknown`-typed request data leaking into typed persistence paths.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Provider mapping admin APIs are now available at `/api/import-provider-mappings*` and ready for the upcoming admin UI story.
---

## 2026-02-17 16:10 CET - US-007
- What was implemented
  - Added provider mapping admin route UI at `/import-provider-mappings` with create/edit/delete flows.
  - Implemented source-column to canonical-field assignment controls with add/remove row support for both create and edit forms.
  - Added normalization-rules JSON editing for create and edit workflows, including client-side JSON validation.
  - Surfaced API validation failures (including `REQUIRED_CANONICAL_FIELDS_MISSING`) as user-facing feedback in the UI.
  - Added Playwright coverage for provider mapping admin create/edit flow and validation feedback.
- Files changed
  - `src/app/import-provider-mappings/page.tsx`
  - `src/app/import-provider-mappings/provider-mappings-manager.tsx`
  - `src/app/layout.tsx`
  - `tests/e2e/us-019-provider-mapping-admin-ui.e2e.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep provider mapping admin interactions consolidated in `src/app/import-provider-mappings/*` and rely on `/api/import-provider-mappings*` for canonical validation and persistence behavior.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Playwright `getByRole("alert")` can match Next.js route-announcer nodes; use text-specific assertions for user-visible error alerts.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Provider mapping UI now appears in primary navigation as `Providers` and supports inline edit of normalization JSON plus field mappings.
---

## 2026-02-17 16:24 CET - US-008
- What was implemented
  - Added a new best-effort OpenAI message cleanup service in `src/lib/import/openai-message-cleanup.ts` for review-stage enrichment.
  - Wired review staging (`src/lib/import/review-stage.ts`) to request optional cleaned-message suggestions and return stable diagnostics via `messageCleanupUnavailableReason`.
  - Ensured parse/review flow remains non-blocking: cleanup failures/timeouts/provider errors fall back to staged rows without interruption.
  - Updated `/api/imports/parse` to pass cleanup runtime config (`OPENAI_API_KEY`, optional `OPENAI_MESSAGE_CLEANUP_ENABLED=false` disable switch) into staging.
  - Added focused unit coverage for cleanup result parsing and unavailable reason handling.
- Files changed
  - `src/lib/import/openai-message-cleanup.ts`
  - `src/lib/import/openai-message-cleanup.test.ts`
  - `src/lib/import/review-stage.ts`
  - `src/lib/import/review-stage.test.ts`
  - `src/app/api/imports/parse/route.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep OpenAI cleanup response contracts explicit in review payloads so UI can distinguish unavailable reasons from row-level no-suggestion outcomes.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Review-stage tests should inject `openAiApiKey` explicitly to avoid environment-dependent expectations.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Cleanup diagnostics now live on parse review payload as `review.messageCleanupUnavailableReason`; row suggestions are exposed as `row.cleanedMessage`.
---

## 2026-02-17 16:30 CET - US-009
- What was implemented
  - Updated `/import` review table UI to show original message text and optional AI-cleaned suggestion per row.
  - Added explicit per-row message source control (`Original message` vs `AI-cleaned message`) in review, with deterministic defaults: choose AI-cleaned when available, otherwise original.
  - Added deterministic UI copy documenting the default selection behavior.
  - Exposed row-level accessible names on review combobox triggers to keep row-specific controls uniquely targetable.
  - Extended import route E2E coverage to verify message rendering, default choice behavior, and user override interaction.
- Files changed
  - `src/app/import/import-uploader.tsx`
  - `tests/e2e/us-016-import-route-upload.e2e.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - In shadcn Select workflows, put row-specific accessible labels on `SelectTrigger` so Playwright can reliably target one combobox when a row has multiple selects.
    - Keep UI-only review decisions local to `/import` state when submit payload contracts are intentionally deferred to a later story.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Broad `getByRole("combobox")` row locators become ambiguous as soon as additional row-level selects are introduced.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Review payload rows already include `title` and optional `cleanedMessage`; this story surfaces them in the table while persistence of message choice remains for `US-010`.
---
## 2026-02-17 18:49 CET - US-010
- What was implemented
  - Extended `/import` review submit payload to include per-row finalized message values as `selectedMessage`, based on each row's explicit message choice (original vs AI-cleaned suggestion).
  - Updated `/api/imports/submit` payload validation to require and parse `rows[{ rowId, categoryId, selectedMessage }]`.
  - Updated review submit persistence (`src/lib/import/review-submit.ts`) to apply submitted message decisions when finalizing rows by recomputing `normalizedMerchant` from staged `name` + `selectedMessage` before fingerprint dedupe and transaction insert.
  - Kept review-session cleanup atomic after persistence and category validation.
  - Added/updated tests to verify submit payload contract and persisted transaction behavior using selected message text.
- Files changed
  - `src/app/import/import-uploader.tsx`
  - `src/app/api/imports/submit/route.ts`
  - `src/lib/import/review-submit.ts`
  - `src/lib/import/review-submit.test.ts`
  - `tests/e2e/us-016-import-route-upload.e2e.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep submit-time review decisions explicit in the API contract so UI-only selection state does not get lost between parse and submit.
    - Recompute normalized fingerprint inputs from finalized review values at submit time to ensure dedupe behavior matches user-approved edits.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - `review-submit` test fixtures should mirror staged normalization shape (`normalizedMerchant` based on `name + title`) or expectations will drift when submit logic re-normalizes from finalized message input.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Message choices are still ephemeral until submit; persistence now happens only through `rows[].selectedMessage` in `/api/imports/submit`.
---
