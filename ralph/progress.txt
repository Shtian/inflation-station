## Codebase Patterns
- Keep primary app navigation in `src/app/layout.tsx`, and keep `/` redirecting to `/overview`.
- Keep API handlers in `src/app/api/**` and use the shared server-only Prisma client in `src/lib/prisma.ts`.
- Use shadcn table primitives from `src/components/ui/table.tsx` for tabular UI instead of ad-hoc table markup.
- For paginated table routes, keep summary metadata (`page`, `totalPages`, `total`) outside the table and keep row rendering inside table primitives.
- Keep route page state client-side and fetch route-specific API data on filter/pagination changes.
- Keep transactions pagination/filter query logic in `src/lib/transactions/*`, and keep `/api/transactions` handlers focused on request validation and HTTP response shaping.
- Keep `/api/transactions/[transactionId]` handlers consistent across mutation methods by validating `transactionId` up front and mapping Prisma `P2025` to `TRANSACTION_NOT_FOUND`.
- Keep transactions route filter state client-side in `src/app/transactions/*`, and refetch `/api/transactions` whenever account, page, or pageSize changes.
- Keep `/api/transactions` list rows enriched with `categoryName` so `/transactions` can render category labels (or uncategorized states) without extra category lookups.
- Keep transaction edit UI state local in `src/app/transactions/*` and refresh list data via the same paginated query after successful saves to preserve active filter/page context.
- Keep transaction delete confirmation state local in `src/app/transactions/*` and refetch the paginated list after delete so the UI can recover from last-row-on-page removals.
- For Next.js dynamic API routes, accept `params` as `Promise<unknown>` in the handler signature and narrow locally before using route ids.
- For Next.js App Router handlers, return `new Response(null, { status: 204 })` for no-content responses; `NextResponse.json` is invalid for HTTP 204.
- Keep Playwright configured for base URL `http://127.0.0.1:3000` and use `PLAYWRIGHT_NO_WEBSERVER=1` when you need to force reuse of an already running local dev server.
- Keep `pnpm test` as the full verification entry point (unit + E2E) and use `pnpm test:unit` for fast Vitest-only loops.

## 2026-02-16 12:46 CET - US-001
- What was implemented
  - Added `GET /api/transactions` with `page`, `pageSize`, and optional `accountId` query support, validation errors for invalid pagination params, and paginated response metadata.
  - Added deterministic transaction pagination query logic with stable ordering (`bookingDate desc`, `id desc`) and normalized API row serialization.
  - Added unit tests covering pagination/filter forwarding, deterministic query arguments, and row serialization.
- Files changed
  - `src/app/api/transactions/route.ts`
  - `src/lib/transactions/list.ts`
  - `src/lib/transactions/list.test.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
  - `src/components/ui/button-group.tsx`
  - `src/components/ui/button.tsx`
  - `src/components/ui/calendar.tsx`
  - `src/components/ui/separator.tsx`
  - `src/lib/utils.ts`
- Quality checks run (and result)
  - `pnpm lint` (pass; required pre-existing Biome fixes were applied first)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep API route handlers thin: parse/validate request input in `src/app/api/**` and move deterministic data query/shape logic into `src/lib/**`.
    - For paginated transaction lists, always include deterministic tie-break ordering after `bookingDate` to keep page boundaries stable.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Biome lint can fail due unrelated formatting drift already present in branch; run targeted `biome check --write` on reported files before rerunning required gates.
  - Useful context (e.g., "the evaluation panel is in component X")
    - `pnpm test` runs Vitest unit tests only; Playwright E2E remains under `pnpm test:e2e`.
---
## 2026-02-16 12:51 CET - US-002
- What was implemented
  - Added `PATCH /api/transactions/[transactionId]` to update mutable transaction fields (`categoryId`, `bookingDate`, `amountNok`, `currency`, `normalizedMerchant`, `paymentType`).
  - Added strict update payload validation that rejects immutable transaction metadata fields (`id`, `accountId`, `createdAt`, `updatedAt`) and invalid booking-date formats.
  - Added transaction update library logic to persist updates and return a stable API row shape.
  - Added unit tests for mutable-field parsing, immutable-field rejection, and update persistence/mapping behavior.
- Files changed
  - `src/app/api/transactions/[transactionId]/route.ts`
  - `src/lib/transactions/update.ts`
  - `src/lib/transactions/update.test.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep transaction mutation business logic and row shaping in `src/lib/transactions/*`, with API routes limited to validation and Prisma error-to-HTTP mapping.
    - Use strict payload schemas with explicit immutable-field rejections for edit endpoints that must protect import metadata.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Next.js route type generation can reject narrow `params` handler signatures; keep the signature broad and narrow route params inside the handler.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Transaction update responses currently reuse the list-row shape for consistency with `/api/transactions`.
---
## 2026-02-16 13:00 CET - US-003
- What was implemented
  - Added transaction hard-delete library logic in `src/lib/transactions/delete.ts` and wired `DELETE /api/transactions/[transactionId]` to perform hard deletes by id.
  - Added route-level `transactionId` validation for delete requests and returned `INVALID_TRANSACTION_ID` when params are malformed.
  - Mapped Prisma not-found (`P2025`) to `TRANSACTION_NOT_FOUND` with HTTP 404 for delete operations.
  - Added unit coverage for delete persistence call behavior.
- Files changed
  - `src/app/api/transactions/[transactionId]/route.ts`
  - `src/lib/transactions/delete.ts`
  - `src/lib/transactions/delete.test.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep transaction deletion as a dedicated helper in `src/lib/transactions/*` so API handlers remain focused on parameter validation and HTTP mapping.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Biome formatting checks are strict; run `pnpm exec biome check --write` on edited files if lint fails on formatting-only differences.
  - Useful context (e.g., "the evaluation panel is in component X")
    - The transaction detail API route now supports both `PATCH` and `DELETE`, sharing the same guarded transaction-id parsing path.
---
## 2026-02-16 13:43 CET - US-004
- What was implemented
  - Added a dedicated `/transactions` route and linked it in primary navigation.
  - Implemented client-side account filter, page-size selector, and numeric page controls wired to `GET /api/transactions`.
  - Added browser-level Playwright coverage for account filter and pagination control behavior on `/transactions`.
- Files changed
  - `src/app/layout.tsx`
  - `src/app/transactions/page.tsx`
  - `src/app/transactions/transactions-manager.tsx`
  - `tests/e2e/us-018-transactions-route-controls.e2e.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
  - `pnpm exec playwright test tests/e2e/us-018-transactions-route-controls.e2e.ts` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep route-level state and API wiring in dedicated route manager components (for example `transactions-manager.tsx`) and keep page files as thin wrappers.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Playwright strict locators can collide with Next.js dev-tools UI in dev mode; use exact button-name matching when labels are short.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Transactions route currently renders summary + preview rows from paginated API responses; table/edit/delete flows remain for later stories.
---
## 2026-02-16 13:51 CET - US-005
- What was implemented
  - Replaced transactions preview list rendering with canonical shadcn table primitives (`Table`, `TableHeader`, `TableBody`, `TableRow`, `TableHead`, `TableCell`) on `/transactions`.
  - Kept page metadata and total-count metadata visible while rendering paginated rows.
  - Preserved explicit empty-state messaging when no rows match active filters.
  - Expanded Playwright coverage to assert table headers/metadata and empty-state behavior for account-filtered results.
- Files changed
  - `src/app/transactions/transactions-manager.tsx`
  - `tests/e2e/us-018-transactions-route-controls.e2e.ts`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass; required formatting fix applied to updated E2E spec)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
  - `pnpm exec playwright test tests/e2e/us-018-transactions-route-controls.e2e.ts` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - For transactions list UI, keep pagination metadata (`page`, `totalPages`, `total`) outside the table while using `src/components/ui/table.tsx` primitives for row rendering.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - In this environment, Playwright web server startup may require escalated permissions for local port binding (`EPERM` on `0.0.0.0:3100`).
  - Useful context (e.g., "the evaluation panel is in component X")
    - `tests/e2e/us-018-transactions-route-controls.e2e.ts` now covers both pagination/filter interactions and zero-row filtered state assertions.
---
## 2026-02-16 13:58 CET - US-006
- What was implemented
  - Added row-level `Edit` actions on `/transactions` that open a shadcn `Dialog` with prefilled mutable transaction fields.
  - Wired dialog submit to `PATCH /api/transactions/[transactionId]` with client-side validation for required fields and numeric amount parsing.
  - Refreshed the paginated transaction list after successful save so the current account filter/page state is preserved.
  - Expanded Playwright coverage to validate the modal edit flow and post-save refresh behavior on the active page.
- Files changed
  - `src/components/ui/dialog.tsx`
  - `src/app/transactions/transactions-manager.tsx`
  - `tests/e2e/us-018-transactions-route-controls.e2e.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
  - `pnpm exec playwright test tests/e2e/us-018-transactions-route-controls.e2e.ts` (pass; required escalated permissions for local web server port binding)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep transaction edit interactions in route-local client state and re-fetch through existing paginated query parameters after save rather than mutating table state ad hoc.
    - Add reusable shadcn-style primitives under `src/components/ui/*` when introducing new interaction patterns (for example dialog-based editing).
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Broad Playwright route mocks like `**/api/transactions**` can unintentionally intercept mutation calls; guard by HTTP method and fallback non-GET requests.
  - Useful context (e.g., "the evaluation panel is in component X")
    - The `/transactions` edit form currently supports `bookingDate`, `amountNok`, `currency`, `normalizedMerchant`, and `paymentType` through the existing transaction update API.
---
## 2026-02-16 14:08 CET - US-007
- What was implemented
  - Added row-level `Delete` actions on `/transactions` with a shadcn-style confirmation dialog.
  - Wired confirm to `DELETE /api/transactions/[transactionId]` and cancel to close without mutation.
  - Refetched paginated data after delete so the route falls back to a valid page when deleting the last row on the current page.
  - Expanded Playwright coverage for cancel/confirm behavior and pagination recovery after deletion.
- Files changed
  - `src/components/ui/alert-dialog.tsx`
  - `src/app/transactions/transactions-manager.tsx`
  - `tests/e2e/us-018-transactions-route-controls.e2e.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
  - `pnpm exec playwright test tests/e2e/us-018-transactions-route-controls.e2e.ts --workers=1` (pass; required escalated permissions for local port binding)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Use route-local confirmation state for destructive row actions and reuse a shared `src/components/ui/*` primitive for consistent shadcn dialog behavior.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - For delete-flow Playwright specs, keep GET response pagination metadata aligned with the row set being mocked; otherwise page navigation controls can be unintentionally disabled.
  - Useful context (e.g., "the evaluation panel is in component X")
    - The transactions route now has both edit (`Dialog`) and delete confirmation (`AlertDialog`) flows in `src/app/transactions/transactions-manager.tsx`.
---
## 2026-02-16 14:13 CET - US-008
- What was implemented
  - Added API route unit coverage for `GET /api/transactions` to verify pagination defaults/validation and account-filter forwarding.
  - Added API route unit coverage for `PATCH`/`DELETE /api/transactions/[transactionId]` to verify route param validation, update payload validation+persistence wiring, and delete not-found mapping.
  - Fixed `DELETE /api/transactions/[transactionId]` to return a valid 204 no-content response using `new Response(null, { status: 204 })`.
- Files changed
  - `src/app/api/transactions/route.test.ts`
  - `src/app/api/transactions/[transactionId]/route.test.ts`
  - `src/app/api/transactions/[transactionId]/route.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Add route-level tests alongside transaction lib tests when endpoint behavior includes request parsing, status mapping, or framework-specific response behavior.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - `NextResponse.json` cannot be used with HTTP `204`; tests will fail with `Invalid response status code 204` unless the handler returns an empty `Response`.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Transaction API route tests now live next to handlers in `src/app/api/transactions/**/*.test.ts`, with module mocks for `@/lib/prisma` and transaction service helpers.
---
## 2026-02-16 19:48 CET - US-009
- What was implemented
  - Extended `GET /api/transactions` list-row shaping to include `categoryName` from the related category relation for table rendering.
  - Added a `Category` column in `/transactions` and rendered missing categories as explicit `Uncategorized`.
  - Updated transaction list/update unit tests and refreshed Playwright transaction-route mocks/assertions to include category display behavior and current pagination/action-menu controls.
- Files changed
  - `src/lib/transactions/list.ts`
  - `src/lib/transactions/list.test.ts`
  - `src/lib/transactions/update.ts`
  - `src/lib/transactions/update.test.ts`
  - `src/app/transactions/transactions-manager.tsx`
  - `tests/e2e/us-018-transactions-route-controls.e2e.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
  - `pnpm exec playwright test tests/e2e/us-018-transactions-route-controls.e2e.ts --workers=1` (blocked: Next dev server startup failed due existing `.next/dev/lock`, resulting in `ERR_CONNECTION_REFUSED`)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep transaction list row shape aligned across list and mutation mappers when adding new table-visible fields to avoid type drift in shared `TransactionListRow`.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Playwright webServer startup can fail with stale `.next/dev/lock`; when this happens, browser tests may fail before app routing assertions run.
  - Useful context (e.g., "the evaluation panel is in component X")
    - `/transactions` now expects `categoryName` in API rows and falls back to `Uncategorized` in `src/app/transactions/transactions-manager.tsx`.
## 2026-02-16 19:58 CET - US-010
- What was implemented
  - Added category loading to `/transactions` and wired the edit dialog with a category selector that supports explicit `Uncategorized` (`null`) as well as concrete categories.
  - Extended edit submit payloads to include `categoryId`, and refreshed paginated results now reflect updated category labels in the table.
  - Updated transaction update/API tests and transaction-route Playwright coverage to assert category edit persistence behavior.
  - Added a permanent Playwright/local-dev workflow fix: `playwright.config.ts` now targets port `3000`, can skip managed web server via `PLAYWRIGHT_NO_WEBSERVER`, and `pnpm test` now runs full unit + E2E checks while `pnpm test:unit` remains available for quick loops.
- Files changed
  - `src/app/transactions/transactions-manager.tsx`
  - `src/lib/transactions/update.test.ts`
  - `src/app/api/transactions/[transactionId]/route.test.ts`
  - `tests/e2e/us-018-transactions-route-controls.e2e.ts`
  - `playwright.config.ts`
  - `package.json`
  - `README.md`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm run test:unit` (pass)
  - `pnpm build` (pass)
  - `PLAYWRIGHT_NO_WEBSERVER=1 pnpm test` (blocked in this sandbox: Playwright Chromium launch permission error `bootstrap_check_in ... Permission denied (1100)`)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep transaction-edit form state authoritative in `transactions-manager` and include all mutable fields (including `categoryId`) in one PATCH payload so table refreshes stay deterministic.
    - Use `pnpm test` for full verification and `pnpm test:unit` for fast feedback while iterating on non-browser changes.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - In constrained environments, Playwright browser launch can fail even when the app server is healthy; this is separate from Next.js dev-server lock issues.
    - When `pnpm test` includes E2E, local/browser prerequisites must be available or the command fails after unit tests.
  - Useful context (e.g., "the evaluation panel is in component X")
    - `playwright.config.ts` now supports `PLAYWRIGHT_NO_WEBSERVER=1` to force reuse of a running dev server instead of spawning `pnpm dev`.
---
