## Codebase Patterns
- For replaceable month-scoped AI outputs, model a single row per month with a unique `monthStart` and persisted lifecycle metadata (`status`, `generatedAt`, `errorMessage`, `reviewText`).
- Persist monthly-review system prompt in a singleton table row with a fixed key, and resolve blank/missing prompt text to a default prompt in the backend service layer.
- Keep monthly timeline aggregation in a route-independent service (`src/lib/monthly-review/overview.ts`) that computes spend from negative transactions, includes review-only months, and calculates MoM deltas only when the immediate prior calendar month exists.
- Compose monthly timeline API rows in `src/lib/monthly-review/timeline.ts` so every month includes an explicit review state (`NOT_GENERATED`, `GENERATING`, `GENERATED`, `FAILED`) with review metadata merged onto deterministic overview metrics.
- Build monthly-review AI generation payloads in a dedicated backend service (`src/lib/monthly-review/generation-input.ts`) that validates `monthStart`, resolves prompt fallback centrally, and returns precomputed spend/category/merchant/MoM metrics plus minimal transaction fields.
- For month-level review generation, always persist replace semantics by resetting the month row to `GENERATING` before provider calls and then persisting either `GENERATED` output or `FAILED` with stable unavailable reasons (`key_missing`, `timeout`, `provider_error`).

## 2026-02-19 13:34:03 CET - US-001
- What was implemented
  - Added Prisma persistence model for monthly AI reviews via new `MonthlyReview` model and `MonthlyReviewStatus` enum.
  - Enforced one-record-per-month with unique `monthStart`, with lifecycle metadata fields for generated timestamp and failure details.
  - Generated and applied Prisma migration, then regenerated Prisma Client.
- Files changed
  - `prisma/schema.prisma`
  - `prisma/migrations/20260219123309_add_monthly_review_model/migration.sql`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
  - `ralph/review-mockup.png`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass: unit + Playwright E2E)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Prisma schema changes should be followed by both migration creation and explicit `pnpm exec prisma generate` before type/build checks.
    - Month-keyed review storage should use normalized first-of-month timestamps (`monthStart`) as the unique identity anchor.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Keep lifecycle status as an enum in Prisma for stable API/UI state mapping in later stories.
  - Useful context (e.g., "the evaluation panel is in component X")
    - The new table/index are created in `prisma/migrations/20260219123309_add_monthly_review_model/migration.sql` and ready for upcoming timeline/generation stories.
---
## 2026-02-19 13:39:38 CET - US-002
- What was implemented
  - Added persistence for monthly review system prompt via a new Prisma `MonthlyReviewSystemPrompt` model.
  - Added backend get/update helpers in `src/lib/monthly-review/system-prompt.ts` with stable singleton id access.
  - Implemented fallback behavior so missing or blank persisted prompt resolves to a default monthly review system prompt.
  - Added unit tests covering missing, stored, updated, and blank prompt flows.
- Files changed
  - `prisma/schema.prisma`
  - `prisma/migrations/20260219123825_add_monthly_review_system_prompt/migration.sql`
  - `src/lib/monthly-review/system-prompt.ts`
  - `src/lib/monthly-review/system-prompt.test.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass: unit + Playwright E2E)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Store global monthly-review prompt state as a singleton row keyed by a fixed id to keep reads/writes deterministic.
    - Resolve default prompt behavior in backend prompt service logic, not in callers, so future generation flows reuse one fallback rule.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Blank prompt updates should normalize to `null` persistence, otherwise fallback behavior can diverge between update and read paths.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Prompt helper lives in `src/lib/monthly-review/system-prompt.ts` and currently exposes both resolved prompt text and whether fallback default was used.
---
## 2026-02-19 13:51:48 CET - US-003
- What was implemented
  - Added deterministic monthly overview service in `src/lib/monthly-review/overview.ts` with per-month totals for spend, spend transaction count, top spend category, and month-over-month delta.
  - Implemented month row materialization from both transaction months and `MonthlyReview.monthStart` values so months are returned even without AI review text or transactions.
  - Added unit tests validating deterministic ordering, spend-only aggregation, top-category tie behavior, review-only month inclusion, and query-shape expectations.
- Files changed
  - `src/lib/monthly-review/overview.ts`
  - `src/lib/monthly-review/overview.test.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass: unit + Playwright E2E)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Monthly spend metrics should be computed from negative transaction amounts only, with absolute values for reporting totals.
    - Top-category selection needs deterministic tie-breaking (`spend desc`, then `categoryName asc`, then `categoryId asc`) to keep API/UI outputs stable.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Month-over-month deltas should only be emitted when the immediate prior calendar month exists in the dataset; otherwise return `null`.
  - Useful context (e.g., "the evaluation panel is in component X")
    - The new service is route-independent and ready for the upcoming timeline API story to compose review state metadata on top.
---
## 2026-02-19 13:57:52 CET - US-004
- What was implemented
  - Added `src/lib/monthly-review/timeline.ts` to compose deterministic monthly overview rows with persisted monthly-review metadata and explicit review states (`NOT_GENERATED`, `GENERATING`, `GENERATED`, `FAILED`).
  - Added `/api/monthly-review/timeline` read endpoint with optional `limit` validation and stable HTTP error mapping for invalid input and server failures.
  - Added unit coverage for timeline composition/state mapping and API route validation/error behavior.
- Files changed
  - `src/lib/monthly-review/timeline.ts`
  - `src/lib/monthly-review/timeline.test.ts`
  - `src/app/api/monthly-review/timeline/route.ts`
  - `src/app/api/monthly-review/timeline/route.test.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass: unit + Playwright E2E)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep monthly timeline route handlers thin by delegating merge logic (overview + review state) to `src/lib/monthly-review/timeline.ts`.
    - Normalize month identity to `YYYY-MM-01` keys when joining overview rows and `MonthlyReview` records.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Overview-derived month-over-month deltas can be non-null for months without review rows when the immediate prior month has spend totals.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Route tests for the new endpoint are in `src/app/api/monthly-review/timeline/route.test.ts`; timeline merge behavior tests are in `src/lib/monthly-review/timeline.test.ts`.
---
## 2026-02-19 14:07:45 CET - US-005
- What was implemented
  - Added `src/lib/monthly-review/generation-input.ts` to build month-scoped AI generation input with validated `monthStart`, resolved system prompt (stored or default), and deterministic precomputed metrics.
  - Included required precomputed metrics in payload output: monthly spend totals, category totals with shares, merchant concentration indicators (top merchants + concentration shares), and month-over-month deltas when immediate prior month rows exist.
  - Returned full selected-month transaction payload rows with minimal required fields (`bookingDate`, `amountNok`, `normalizedMerchant`, `paymentType`, `categoryName`) and deterministic ordering.
  - Adjusted `src/lib/monthly-review/system-prompt.ts` typing to allow read-only prompt clients for generation input builders.
  - Added unit coverage in `src/lib/monthly-review/generation-input.test.ts` for prompt fallback, metrics shape, MoM behavior, minimal transaction payload shape, and invalid month validation.
- Files changed
  - `src/lib/monthly-review/generation-input.ts`
  - `src/lib/monthly-review/generation-input.test.ts`
  - `src/lib/monthly-review/system-prompt.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass: unit + Playwright E2E)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep monthly AI generation payload building route-independent and deterministic so generation APIs can remain thin orchestration layers.
    - Reuse month key validation (`YYYY-MM-01`) and UTC month boundaries in one place to avoid off-by-one and locale-timezone regressions.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - `getMonthlyReviewSystemPrompt` should accept read-only DB clients; requiring write-capable clients creates unnecessary coupling for pure payload builders.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Generation payload tests are localized to `src/lib/monthly-review/generation-input.test.ts` and cover metric arithmetic and payload field minimization.
---
## 2026-02-19 14:16:10 CET - US-006
- What was implemented
  - Added month-level manual generation orchestration in `src/lib/monthly-review/generation.ts` that validates `monthStart`, resets month rows to `GENERATING`, and applies replace semantics by persisting either `GENERATED` review output or `FAILED` state.
  - Integrated OpenAI completion call with deterministic prompt input from `buildMonthlyReviewGenerationInput`, and mapped unavailable/failure outcomes to stable reason codes: `key_missing`, `timeout`, `provider_error`.
  - Added `POST /api/monthly-review/generate` endpoint with stable payload validation and error mapping (`INVALID_MONTHLY_REVIEW_GENERATE_PAYLOAD`, `INVALID_MONTH_START`, `MONTHLY_REVIEW_GENERATION_FAILED`).
  - Added unit coverage for generation service and API route behavior (success, missing key, provider failure, timeout, validation failure).
- Files changed
  - `src/lib/monthly-review/generation.ts`
  - `src/lib/monthly-review/generation.test.ts`
  - `src/app/api/monthly-review/generate/route.ts`
  - `src/app/api/monthly-review/generate/route.test.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass: unit + Playwright E2E)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep monthly review generation routes thin and move replace-state orchestration plus provider handling into `src/lib/monthly-review/generation.ts` so timeline consumers read persisted states consistently.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - On regenerate, clear stale `reviewText`/`generatedAt`/`errorMessage` when entering `GENERATING`; otherwise old output can linger if provider calls fail.
  - Useful context (e.g., "the evaluation panel is in component X")
    - API route tests for generation are in `src/app/api/monthly-review/generate/route.test.ts`; provider/key failure behavior is covered in `src/lib/monthly-review/generation.test.ts`.
---
