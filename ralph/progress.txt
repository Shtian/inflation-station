## Codebase Patterns
- Keep primary app navigation in `src/app/layout.tsx`, and keep `/` redirecting to `/overview`.
- Keep API handlers in `src/app/api/**` and use the shared server-only Prisma client in `src/lib/prisma.ts`.
- Use shadcn table primitives from `src/components/ui/table.tsx` for tabular UI instead of ad-hoc table markup.
- For paginated table routes, keep summary metadata (`page`, `totalPages`, `total`) outside the table and keep row rendering inside table primitives.
- Keep route page state client-side and fetch route-specific API data on filter/pagination changes.
- Keep transactions pagination/filter query logic in `src/lib/transactions/*`, and keep `/api/transactions` handlers focused on request validation and HTTP response shaping.
- Keep `/api/transactions/[transactionId]` handlers consistent across mutation methods by validating `transactionId` up front and mapping Prisma `P2025` to `TRANSACTION_NOT_FOUND`.
- Keep transactions route filter state client-side in `src/app/transactions/*`, and refetch `/api/transactions` whenever account, page, or pageSize changes.
- Keep `/api/transactions` list rows enriched with `categoryName` so `/transactions` can render category labels (or uncategorized states) without extra category lookups.
- Keep transaction edit UI state local in `src/app/transactions/*` and refresh list data via the same paginated query after successful saves to preserve active filter/page context.
- Keep the transactions edit workflow NOK-only by removing currency from edit payload parsing/UI and forcing persisted updates to `currency: "NOK"`.
- Keep transaction delete confirmation state local in `src/app/transactions/*` and refetch the paginated list after delete so the UI can recover from last-row-on-page removals.
- For Next.js dynamic API routes, accept `params` as `Promise<unknown>` in the handler signature and narrow locally before using route ids.
- For Next.js App Router handlers, return `new Response(null, { status: 204 })` for no-content responses; `NextResponse.json` is invalid for HTTP 204.
- Keep Playwright configured for base URL `http://127.0.0.1:3000` and use `PLAYWRIGHT_NO_WEBSERVER=1` when you need to force reuse of an already running local dev server.
- Keep `pnpm test` as the full verification entry point (unit + E2E) and use `pnpm test:unit` for fast Vitest-only loops.
- Store provider import mappings in Prisma using a parent mapping model plus child field-mapping rows, with JSON normalization/transform payloads and `mappingVersion` metadata for diagnostics.
- For provider autodetection, read configurable `requiredHeaders`/`anyHeaders`/`headerPatterns` from mapping JSON, fall back to mapped source fields, and return parse diagnostics with a `certain`/`uncertain`/`missing` confidence state.
- When `/api/imports/parse` detection is not `certain` and provider candidates exist, return `PROVIDER_SELECTION_REQUIRED` with diagnostics and require a follow-up parse call with explicit `providerId`.

## 2026-02-17 13:53 CET - US-001
- What was implemented
  - Added Prisma models for provider mapping definitions: `ImportProviderMapping` (provider name, normalization rules JSON, mapping version metadata) and `ImportProviderFieldMapping` (source-to-canonical mappings plus optional transform rules JSON).
  - Added relational constraints/indexes for deterministic lookup and canonical-field uniqueness per provider mapping.
  - Generated and applied migration `20260217125021_add_import_provider_mappings`, then regenerated Prisma client.
  - Fixed pre-existing lint/typecheck/test drift required to pass repo quality gates (`src/components/ui/tabs.tsx`, `src/lib/deterministic-color.ts`, and dashboard E2E tab assertions).
- Files changed
  - `prisma/schema.prisma`
  - `prisma/migrations/20260217125021_add_import_provider_mappings/migration.sql`
  - `src/components/ui/tabs.tsx`
  - `src/lib/deterministic-color.ts`
  - `tests/e2e/us-012-category-review-ui.e2e.ts`
  - `tests/e2e/us-014-dashboard-ui.e2e.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm exec prisma generate` (pass)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep provider mapping schema split between provider-level metadata and per-field mapping rows so detection/parse logic can evolve without schema churn.
    - Preserve mapping diagnostics in schema with explicit version metadata (`mappingVersion`) and timestamps.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Overview dashboard assertions in Playwright must switch tabs before expecting category-tab content; those headings/charts are not visible in the default `Flow` tab.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Prisma migration `20260217125021_add_import_provider_mappings` is now the baseline for upcoming provider detection and mapping API stories.
---

## 2026-02-17 14:28 CET - US-002
- What was implemented
  - Added provider detection service in `src/lib/import/provider-detection.ts` that evaluates mappings with configurable header/pattern rules and score-based confidence.
  - Wired `/api/imports/parse` to return `detection` diagnostics with detected provider id/name, confidence state, score, matched headers, and ranked candidates.
  - Added focused unit coverage for certain/uncertain/missing detection and source-field fallback behavior.
- Files changed
  - `src/lib/import/provider-detection.ts`
  - `src/lib/import/provider-detection.test.ts`
  - `src/app/api/imports/parse/route.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep provider detection logic in `src/lib/import/*` and keep `/api/imports/parse` focused on payload validation plus response shaping.
    - Keep detection confidence deterministic by combining required-header coverage with optional/pattern signal and ranking candidates.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - CSV files without a recognizable header row should return `missing` detection diagnostics instead of forcing a low-confidence provider guess.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Detection currently informs diagnostics only; canonical row transformation is still handled by the existing parser and staging flow.
---

## 2026-02-17 14:54 CET - US-003
- What was implemented
  - Extended `/api/imports/parse` payload parsing to accept optional `providerId` from both multipart form-data and JSON.
  - Updated parse flow to run provider detection first, return `409 PROVIDER_SELECTION_REQUIRED` with diagnostics when detection is `uncertain`/`missing` and candidates exist, and only stage review rows once parse is allowed.
  - Added provider override handling in parse route: validates selected provider, uses it instead of auto-detection, and returns the selected provider in diagnostics.
  - Updated `/import` UI to include provider selection (auto-detect + detected candidates), show detection status, and re-parse with override when required.
  - Added Playwright E2E coverage for uncertain detection requiring manual provider selection before parsing can continue.
- Files changed
  - `src/app/api/imports/parse/route.ts`
  - `src/app/import/import-uploader.tsx`
  - `tests/e2e/us-016-import-route-upload.e2e.ts`
  - `AGENTS.md`
  - `ralph/prd.json`
  - `ralph/progress.txt`
- Quality checks run (and result)
  - `pnpm lint` (pass)
  - `pnpm exec tsc --noEmit` (pass)
  - `pnpm test` (pass)
  - `pnpm build` (pass)
- **Learnings for future iterations**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep ambiguous-provider handling in the parse API contract (`PROVIDER_SELECTION_REQUIRED` + detection payload) so UI can gate progression deterministically.
    - Keep provider override state in `/import` client state and submit it via multipart `providerId` to avoid introducing separate provider preflight endpoints.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - `/import` parse responses now require a `detection` payload; mocked API responses in E2E tests must include it to avoid runtime failures.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Existing import review/submit flow remains unchanged once parse succeeds; this story only gates parse progression when provider detection is ambiguous.
---
